{
    "git": {
        "deploymentEnabled": {
            "master": true,
            "dev": false
        }
    },
    "buildCommand": "yarn install --silent && yarn build",
    "installCommand": "yarn install --silent"
}
