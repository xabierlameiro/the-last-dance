{
    "functions": {
        "pages/api/**/*.js": {
            "runtime": "nodejs22.x"
        }
    },
    "git": {
        "deploymentEnabled": {
            "master": true,
            "dev": false
        }
    },
    "buildCommand": "yarn install --silent && yarn build",
    "installCommand": "yarn install --silent"
}
