const locales = ['en', 'es', 'gl'];
import { messages } from '../src/intl/translations';

const formats = {};

export const reactIntl = {
    defaultLocale: 'en',
    locales,
    messages,
    formats,
};
