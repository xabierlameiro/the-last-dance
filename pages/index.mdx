import Layout from "@/Layout";

export const meta = {
    author: "Xabier Lameiro",
    title: "My personal website",
};

<CH.Code lineNumbers={true} style={{ width:'100%', maxWidth: '750px', margin:'100px auto'}}>

```js index.tsx
import React from "react";

type Props = {
    name?: string,
};

const App = ({ name = "Xabier Lameiro" }: Props) => {
    return (
        <h1>
            "Hello, my name is {name}! and I'm Frontend developer. \n I was born
            and raised in Spain but currently live in Ireland, \n trying to
            improve my English. I like web development, applications, \n
            automations and the Iot."
        </h1>
    );
};

export { App, App as default };
```

```css knowledge.css
.experience {
    Plexus Tech / Spain: 2 years !important;
    Orienteed / Spain: 1.5 years;
    Getronics / Spain:  .3 years;
    Viewnext / Spain : 1.5 years;
    Interlinea srl / Italy :  .4 years;
}

.education {
    Superior Technician in Web Applications Development : 2 years;
    Technician in Microcomputer and Networks  : 2 years;
}
```

```json contact.json
{
    "email": "xabier.lameiro@gmail.com"
}
```

</CH.Code>

export default ({ children }) => <Layout meta={meta}>{children}</Layout>;

;
