import Layout from '@/layout';
import Dialog from '@/components/Dialog';

const Page = () => {
    const meta = {
        title: "This is the Xabier's blog",
    };
    return (
        <Layout meta={meta}>
            <Dialog>
                <></>
            </Dialog>
        </Layout>
    );
};

export default Page;
